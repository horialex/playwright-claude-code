export enum EmployeeRowAction {
    ACTIVATE = 'Activează',
    DEACTIVATE = 'Dezactivează',
}

export enum EmployeeColumn {
    USERNAME = 'Nume utilizator',
    EMAIL = 'Email',
    LAST_EDITED = 'Ultima editare',
    STATUS = 'Status',
}
